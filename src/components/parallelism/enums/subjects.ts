export enum Subjects {
  FLYNNS_TAXONOMY = 'flynns-taxonomy',
  WHEN_TO_USE_PARALLELISM = 'when-to-use-parallelism',
  PARALLELISM_VS_CONCURRENCE = 'parallelism-vs-concurrence',
  CPU_VS_GPU = 'cpu-vs-gpu',
}
